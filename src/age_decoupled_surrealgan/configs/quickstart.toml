experiment_name = "age-decoupled-surrealgan-quickstart"

[training]
repetitions = 1
epochs = 5
batch_size = 64
save_every = 0
target_regular_checkpoints = 2
console_log_every = 1
persistent_workers = false

[model]
n_processes = 3
